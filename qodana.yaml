version: "1.0"
linter: jetbrains/qodana-js:2025.2

profile:
  name: qodana.recommended

include:
  - name: CheckDependencyLicenses

coverage:
  include:
    - coverage
  exclude:
    - node_modules
    - dist
    - build

projectJDK: 21
failThreshold: 0