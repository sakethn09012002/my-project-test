---
name: Push on Main branch
on:
  push:
    branches:
      - main

jobs:
  build:
    name: Build stage
    uses: ./.github/workflows/build.yaml
    secrets: inherit
    with:
      docker-tag: latest
